#!/bin/bash

JAVA_OPTS="-Xms256M -Xmx2048M"
PROCESS_CONTROLLER_JAVA_OPTS="\$JAVA_OPTS"
HOST_CONTROLLER_JAVA_OPTS="\$JAVA_OPTS"
export LANG=hu_HU

for PROP in ${WILDFLY_BASE}/domain-controller/configuration/properties/*.properties
do
    cat \$PROP > ${WILDFLY_BASE}/domain-controller/configuration/wildfly.properties
done
